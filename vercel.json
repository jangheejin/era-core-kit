{
  "buildCommand": "pnpm install && pnpm --filter site run build",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/site/.next",
  "framework": "nextjs",
  "build": {
    "env": {
      "NODE_VERSION": "20"
    }
  }
}